define([
    'backbone'
], function (Backbone) {
    
    var ScoreModel = Backbone.Model.extend({
        initialize: function () {
            
        }
    });
    
    return new ScoreModel();

});