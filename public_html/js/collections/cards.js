define([
    'backbone'
], function (Backbone) {
    var CardCollection = Backbone.Collection.extend({
        initialize: function () {
            
        }
    });
    
    return new CardCollection();
});