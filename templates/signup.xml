<fest:template xmlns:fest="http://fest.mail.ru">
    <fest:set name="signup">
        <div class="signup">
            <fest:get name="form">{type: 'signup'}</fest:get>
        </div>
    </fest:set>
</fest:template>
